.outline-install-template: 
  stage: install
  script:
    - API_PORT=$(jq -r '.values.outputs.randomized_ports.value[0]' ${INFO_FILE})
    - KEYS_PORT=$(jq -r '.values.outputs.randomized_ports.value[1]' ${INFO_FILE})
    - echo "Sending script file to remote instance"
    - scp scripts/outline/install-outline.sh instance:.
    - ssh instance "
        API_PORT=$API_PORT
        KEYS_PORT=$KEYS_PORT
        bash install-outline.sh
      "
    - scp instance:./outline/outline-api.json .
    - mkdir ${CI_PROJECT_DIR}/outline && mv outline-api.json ${CI_PROJECT_DIR}/outline
    - echo "Cleaning up install script from remote instance"
    - ssh instance "rm install-outline.sh"
  artifacts:
    paths:
      - ${CI_PROJECT_DIR}/outline
    expire_in: 30 days

.outline-key-template:
  stage: generate
  image: fureasu346/pipeline-util:v1
  variables:
    KEY_NAMES: "outline-${CI_JOB_ID}"
  script:
    - echo "Creating keys/outline"
    - mkdir -p ${CI_PROJECT_DIR}/keys/outline
    - echo "Sending script file to remote instance"
    - scp scripts/outline/get-outline-key.sh instance:.
    - IFS=',' read -ra KEYS <<< "$KEY_NAMES"
    - |
      for KEY in "${KEYS[@]}"; do
        echo "Generating key for $KEY"
        ssh instance "KEY_NAME=$KEY bash get-outline-key.sh"
        echo "Getting the key file from remote instance"
        scp instance:~/keys/outline/$KEY.json ${CI_PROJECT_DIR}/keys/outline/$KEY.json
      done
    - INFO_FILE="${CI_PROJECT_DIR}/tofu/${PROVIDER}/${CI_PIPELINE_ID}.json"
    - INSTANCE_IP=$(jq -r '.values.outputs.instance_ip.value' ${INFO_FILE})
    - echo "Sending environment information to telegram..."
    - |
      curl -s -X POST \
        -d chat_id="${TELEGRAM_CHAT_ID}" \
        -d parse_mode="HTML" \
        -d text="<b>Provider</b> ==> <code>${PROVIDER}</code>
      <b>Region</b> ==> <code>${REGION}</code>
      <b>PIPELINE_ID</b> ==> <code>${CI_PIPELINE_ID}</code>
      <b>Instance_IP</b> ==> <code>${INSTANCE_IP}</code>
      <b>VPN</b> ==> Outline
      <i>Keys are as below:</i>" \
        https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage > /dev/null
    - echo "Sending keys to telegram..."
    - declare -a KEY_FILES=($(ls ${CI_PROJECT_DIR}/keys/outline))
    - |
      for KEY_FILE in "${KEY_FILES[@]}"; do
        KEY_NAME=$(jq -r '.name' ${CI_PROJECT_DIR}/keys/outline/${KEY_FILE})
        KEY=$(jq -r '.accessUrl' ${CI_PROJECT_DIR}/keys/outline/${KEY_FILE})
        echo "Sending key for ${KEY_NAME}"
        curl -s -X POST \
          -d chat_id="${TELEGRAM_CHAT_ID}" \
          -d text="${KEY_NAME}" \
          https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage > /dev/null
        curl -s -X POST \
          -d chat_id="${TELEGRAM_CHAT_ID}" \
          -d text="${KEY}" \
          https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage > /dev/null
        sleep 2
      done
    - echo "Successfully sent keys to telegram"
    - echo "Cleaning script file from remote instance"
    - ssh instance "rm get-outline-key.sh"
  artifacts:
    paths:
      - ${CI_PROJECT_DIR}/keys/outline
    expire_in: 30 days
